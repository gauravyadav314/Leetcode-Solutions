class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int n = nums.size();
        // vector<int> ans(n);
        // int odd = 0, even = 0;
        // for(int i=0; i<n; i++) {
        //     if(nums1[i]%2==0) even++;
        //     else odd++;
        // }
        // if(odd==n || even==n) return true;
        return true;
         
    }
};